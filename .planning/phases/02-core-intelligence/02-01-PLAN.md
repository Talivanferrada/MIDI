---
phase: 02-core-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - midi-framework/templates/base/.midi/agents/midi-orchestrator.md
  - midi-framework/templates/base/.midi/config/agent-routing.json
  - midi-framework/templates/base/.midi/config/scoring-rubric.json
autonomous: true
requirements: [INT-01]

must_haves:
  truths:
    - "Orchestrator can read PROJECT_STATE.md and determine next step"
    - "Orchestrator applies decision tree correctly"
    - "agent-routing.json has all routing rules"
    - "scoring-rubric.json has weights for all dimensions"
  artifacts:
    - path: "templates/base/.midi/agents/midi-orchestrator.md"
      provides: "Main orchestration agent"
      contains: "decision tree"
    - path: "templates/base/.midi/config/agent-routing.json"
      provides: "Agent routing rules"
    - path: "templates/base/.midi/config/scoring-rubric.json"
      provides: "Scoring weights"
---

<objective>
Implement the orchestrator agent and configuration files with routing rules and scoring rubrics.

Purpose: Create the brain of MIDI that coordinates all other agents and workflows.
Output: Complete orchestrator with decision tree, routing rules, and scoring system.
</objective>

<execution_context>
@/home/vm-labs/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/midi/02_AGENT_ARCHITECTURE.md
@docs/midi/07_PHASE_2_PROMPT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete midi-orchestrator agent</name>
  <files>midi-framework/templates/base/.midi/agents/midi-orchestrator.md</files>
  <action>
    Rewrite the orchestrator agent with complete instructions. Must include:
    
    1. **Role & Purpose**: Master coordinator of MIDI workflow
    
    2. **Decision Tree**: 
       - If no USER_CONTEXT.md → activate intake-agent
       - If no global research → activate global-research-agent
       - If no local research → activate local-adaptation-agent
       - If no benchmark → activate benchmark-agent
       - If no insights → activate insight-agent
       - If less than 10 ideas → activate creative-agent
       - If no top 3 → activate hybridization-agent + evaluator
       - If idea selected → activate financeable mode agents
       - If devil-advocate not run → BLOCK final document
       - If evaluation < 70 → recommend iteration
       - If funding_focus=true → activate funding-match + application-optimizer
    
    3. **Activation Conditions**: Triggered by /midi-start or any workflow command
    
    4. **Blocking Rules**:
       - Cannot generate FINAL_PROJECT_DOCUMENT.md without:
         - Devil advocate report
         - Financial analysis
         - Legal analysis (if applicable)
         - Evaluation scorecard
    
    5. **Human Gates**: When to pause for user approval
    
    6. **State Management**: How to read/update PROJECT_STATE.md
    
    Reference: docs/midi/02_AGENT_ARCHITECTURE.md section on orchestrator
  </action>
  <verify>
    <automated>grep -q "decision tree\|Decision Tree" midi-framework/templates/base/.midi/agents/midi-orchestrator.md && echo "OK" || echo "FAIL"</automated>
  </verify>
  <done>Orchestrator has complete decision tree and blocking rules</done>
</task>

<task type="auto">
  <name>Task 2: Complete agent-routing.json</name>
  <files>midi-framework/templates/base/.midi/config/agent-routing.json</files>
  <action>
    Create comprehensive agent routing configuration. Must include:
    
    ```json
    {
      "modes": {
        "exploration": {
          "sequence": ["intake", "global-research", "local-adaptation", "benchmark", "insight", "creative", "hybridization"],
          "required_outputs": ["USER_CONTEXT.md", "global_research.md", "insights.md", "IDEA_BACKLOG.md", "TOP3_IDEAS.md"],
          "human_gates": ["after_top3"]
        },
        "financeable": {
          "sequence": ["market", "business-model", "technical", "financial", "legal-tax", "risk", "devil-advocate", "validation", "funding-match", "evaluator", "application-optimizer", "writer"],
          "required_outputs": ["market_analysis.md", "business_model_canvas.md", "financial_analysis.md", "devil_report.md", "evaluator_scorecard.md"],
          "human_gates": ["after_devil_advocate", "after_evaluation"],
          "blocking_conditions": ["devil_advocate_not_run", "evaluation_below_70"]
        }
      },
      "activation_rules": {
        "intake": { "trigger": "no_user_context", "priority": 1 },
        "global-research": { "trigger": "no_global_research", "priority": 2 },
        "devil-advocate": { "trigger": "idea_selected", "required": true, "blocking": true },
        "legal-tax": { "trigger": "permits_or_company_or_health_or_food_or_data_or_tourism_or_finance" },
        "funding-match": { "trigger": "funding_focus_true" },
        "evaluator": { "trigger": "all_analysis_complete", "required": true }
      },
      "return_rules": {
        "innovative_but_not_executable": "hybridization",
        "profitable_but_not_innovative": "creative",
        "postulable_but_weak_commercial": "business-model",
        "legal_risk_high": "legal-tax",
        "validation_low": "validation",
        "narrative_weak": "application-optimizer"
      },
      "blocking_rules": {
        "final_document": ["devil_advocate_not_run", "no_financial_analysis", "no_evaluation", "evaluation_below_70"],
        "financeable_mode": ["no_idea_selected"]
      }
    }
    ```
  </action>
  <verify>
    <automated>node -e "const j=require('./midi-framework/templates/base/.midi/config/agent-routing.json'); console.log(Object.keys(j.modes||{}).length)" 2>/dev/null | grep -q "2" && echo "OK" || echo "FAIL"</automated>
  </verify>
  <done>agent-routing.json has complete routing rules with modes, activation rules, and blocking conditions</done>
</task>

<task type="auto">
  <name>Task 3: Complete scoring-rubric.json</name>
  <files>midi-framework/templates/base/.midi/config/scoring-rubric.json</files>
  <action>
    Create comprehensive scoring rubric with two scoring systems:
    
    1. **idea_exploration_score** (for top 3 selection):
       - innovación: 20%
       - interés del usuario: 15%
       - facilidad de validación: 10%
       - potencial comercial: 20%
       - adaptabilidad local: 10%
       - diferenciación: 15%
       - alineación con recursos: 10%
    
    2. **financeable_project_score** (for final evaluation):
       - problema: 8%
       - solución: 8%
       - innovación: 10%
       - mercado: 10%
       - modelo de negocio: 10%
       - factibilidad técnica: 8%
       - factibilidad financiera: 12%
       - legal/tributario: 8%
       - impacto: 6%
       - escalabilidad: 6%
       - equipo/encaje: 5%
       - narrativa: 5%
       - postulabilidad: 4%
       - riesgo: 0% (modifier)
    
    Include:
    - Evaluation criteria for each dimension
    - Score ranges (0-100 per dimension)
    - Passing thresholds (exploration: 60, financeable: 70)
    - Recommendation rules based on total score
  </action>
  <verify>
    <automated>node -e "const j=require('./midi-framework/templates/base/.midi/config/scoring-rubric.json'); console.log(Object.keys(j.idea_exploration_score?.dimensions||{}).length)" 2>/dev/null | grep -q "[5-9]" && echo "OK" || echo "FAIL"</automated>
  </verify>
  <done>scoring-rubric.json has both scoring systems with weights and criteria</done>
</task>

</tasks>

<verification>
1. Orchestrator has decision tree
2. agent-routing.json validates as JSON
3. scoring-rubric.json has both scoring systems
</verification>

<success_criteria>
- Orchestrator can determine next step based on state
- Routing rules cover all activation conditions
- Scoring has weights totaling 100% for each system
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-intelligence/02-01-SUMMARY.md`
</output>
