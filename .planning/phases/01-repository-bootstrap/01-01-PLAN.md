---
phase: 01-repository-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - midi-framework/package.json
  - midi-framework/bin/midi.js
  - midi-framework/src/cli/init.js
  - midi-framework/src/cli/doctor.js
  - midi-framework/src/cli/prompts.js
  - midi-framework/src/cli/logger.js
  - midi-framework/src/installer/createStructure.js
  - midi-framework/src/installer/copyTemplates.js
  - midi-framework/src/installer/detectPlatform.js
  - midi-framework/src/installer/writeConfig.js
  - midi-framework/src/installer/validateInstall.js
  - midi-framework/src/utils/fileSystem.js
  - midi-framework/src/utils/pathUtils.js
autonomous: true
requirements: [BOOT-01, BOOT-02]

must_haves:
  truths:
    - "User can run `midi init` to initialize a new MIDI project"
    - "User can run `midi init --yes` to skip prompts"
    - "User can run `midi doctor` to check installation"
    - "CLI shows version with `midi --version`"
  artifacts:
    - path: "midi-framework/bin/midi.js"
      provides: "CLI entry point"
      exports: ["program"]
    - path: "midi-framework/src/cli/init.js"
      provides: "Init command implementation"
    - path: "midi-framework/src/cli/doctor.js"
      provides: "Doctor command implementation"
  key_links:
    - from: "bin/midi.js"
      to: "src/cli/init.js"
      via: "import"
      pattern: "import init from"
    - from: "src/cli/init.js"
      to: "src/installer/*.js"
      via: "orchestration"
      pattern: "createStructure|copyTemplates|writeConfig"
---

<objective>
Build the core CLI infrastructure with working init and doctor commands.

Purpose: Establish the foundation for MIDI framework with a functional CLI that can initialize projects and validate installations.
Output: Working CLI with init, doctor, and version commands.
</objective>

<execution_context>
@/home/vm-labs/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/vm-labs/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/midi/06_PHASE_1_PROMPT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix and complete CLI entry point</name>
  <files>midi-framework/bin/midi.js</files>
  <action>
    Complete the CLI entry point at `bin/midi.js`. Ensure:
    - Proper shebang for Node.js execution
    - Import and register init and doctor commands
    - Add version command that reads from package.json
    - Add help text in Spanish describing MIDI
    
    The file already exists but verify all imports work correctly.
  </action>
  <verify>
    <automated>node midi-framework/bin/midi.js --version</automated>
  </verify>
  <done>CLI shows version "0.1.0" when running --version flag</done>
</task>

<task type="auto">
  <name>Task 2: Complete init command with full prompts</name>
  <files>midi-framework/src/cli/init.js, midi-framework/src/cli/prompts.js</files>
  <action>
    Complete the init command implementation:
    
    1. In `prompts.js`: Create interactive prompts using inquirer for:
       - project_name (string, required)
       - country (string, default: "Chile")
       - region (string, optional)
       - mode (choices: "full", "exploration", "financeable", default: "full")
       - platform (choices: "opencode", "claude-code", "cursor", "codex", "windsurf", "gemini-cli", "generic")
    
    2. In `init.js`: 
       - Fix the chalk import (should be at top, not bottom)
       - Call getInitOptions with the options object
       - Handle --yes flag to skip prompts and use defaults
       - Implement proper error handling with try/catch
    
    Reference docs/midi/06_PHASE_1_PROMPT.md for exact command options.
  </action>
  <verify>
    <automated>node midi-framework/bin/midi.js init --help</automated>
  </verify>
  <done>Init command accepts all required flags and shows help</done>
</task>

<task type="auto">
  <name>Task 3: Implement doctor command</name>
  <files>midi-framework/src/cli/doctor.js</files>
  <action>
    Implement the doctor command that validates MIDI installation:
    
    Must check:
    1. `.midi/` directory exists
    2. `.midi/config/midi.config.json` exists and is valid JSON
    3. `.midi/agents/` directory exists with at least 5 agent files
    4. `.midi/skills/` directory exists
    5. `.midi/commands/` directory exists
    6. `midi-project/` directory exists
    7. `PROJECT_STATE.md` exists in midi-project
    
    Output a clear status report with checkmarks or X marks for each check.
    Exit with code 0 if all checks pass, 1 otherwise.
  </action>
  <verify>
    <automated>node midi-framework/bin/midi.js doctor || echo "Exit code: $?"</automated>
  </verify>
  <done>Doctor command runs and checks for all required directories and files</done>
</task>

<task type="auto">
  <name>Task 4: Implement installer modules</name>
  <files>midi-framework/src/installer/createStructure.js, midi-framework/src/installer/copyTemplates.js, midi-framework/src/installer/detectPlatform.js, midi-framework/src/installer/writeConfig.js, midi-framework/src/installer/validateInstall.js</files>
  <action>
    Implement all installer modules:
    
    1. `createStructure.js`: Create the full directory structure:
       - .midi/config/, .midi/agents/, .midi/skills/, .midi/commands/, .midi/templates/, .midi/workflows/, .midi/docs/
       - midi-project/ with all subdirectories (00_intake through 13_final)
    
    2. `copyTemplates.js`: Copy template files from templates/base/ to target directory
    
    3. `detectPlatform.js`: Detect agent platform by checking for:
       - CLAUDE.md → claude-code
       - .cursorrules → cursor
       - .windsurfrules → windsurf
       - .codex → codex
       - Return 'generic' if none detected
    
    4. `writeConfig.js`: Write midi.config.json with user answers
    
    5. `validateInstall.js`: Validate that installation completed correctly
    
    All modules should use fs-extra for async file operations.
  </action>
  <verify>
    <automated>node -e "import('./midi-framework/src/installer/createStructure.js').then(m => console.log(typeof m.createStructure))" 2>/dev/null | grep -q function && echo "OK" || echo "FAIL"</automated>
  </verify>
  <done>All installer modules exported and functional</done>
</task>

</tasks>

<verification>
After all tasks:
1. Run `node midi-framework/bin/midi.js --version` - should show 0.1.0
2. Run `node midi-framework/bin/midi.js init --help` - should show all options
3. Run `node midi-framework/bin/midi.js doctor --help` - should show description
</verification>

<success_criteria>
- CLI entry point works with --version flag
- Init command has all required options from specification
- Doctor command validates installation
- All installer modules are implemented and exportable
</success_criteria>

<output>
After completion, create `.planning/phases/01-repository-bootstrap/01-01-SUMMARY.md`
</output>
